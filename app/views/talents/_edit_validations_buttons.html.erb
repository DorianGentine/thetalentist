<div class="form-actions">
  <% if f.object.is_a?(Talent) %>
    <%= link_to talent_path(f.object), class:"btn-edit red-border red margin-right-30" do %>
      Annuler
    <% end %>
  <% else %>
    <%= link_to headhunter_path(f.object), class:"btn-edit red-border red margin-right-30" do %>
      Annuler
    <% end %>
  <% end %>
  <%= f.submit "Valider", class: "btn-edit" %>
</div>
