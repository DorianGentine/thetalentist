<div class="col-lg-3 col-xs-12 margin-bottom-15-xs">
  <%= u.input :job_id, collection: Job.all, label: false, :input_html => { id: "premier-domaine", style: "width: 100%", class: "select2 premier-domaine" }, error: u.object.errors.messages[:job_id][0] %>
</div>

<%= f.simple_fields_for :talent_second_job do |e| %>
  <div class="col-lg-3 col-xs-12 margin-bottom-15-xs">
    <%= e.input :job_id, collection: Job.all, label: false, :input_html => { style: "width: 100%", class: "select2 second-domaine" }, error: e.object.errors.messages[:job_id][0] %>
  </div>
<% end %>

<div class="col-lg-3 col-xs-12">
  <%= u.input :year, placeholder: "Années d'expérience", label: false, :input_html => { style: "width: 100%", min: "0", :onchange => "zeroInput(this.value, 'yearPositif')" }, error: u.object.errors.messages[:year][0] %>
</div>

<%= u.input :position, as: :hidden %>

<script>
  // const premierDomaine = document.getElementById('premier-domaine')
  // var $test = $(".premier-domaine");
  // $test.on("change", function (e) { log("change"); });
  //   console.log(premierDomaine)
  //   console.log(premierDomaine.value)
  // premierDomaine.addEventListener("change", ()=>{
  //   console.log(event)
  //   console.log(premierDomaine)
  // })
</script>
