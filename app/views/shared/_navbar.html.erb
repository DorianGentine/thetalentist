<noscript style="width: 100vw; top: 70px; left: 0; z-index: 19; padding: 20px;" class=" fixed text-align-center white-background"><h3 class="red">The talentist fonctionne mieux avec javascript, activez-le &#128519;</h3></noscript>
<% if params[:action] != "home" && params[:action] != "talent_home" && params[:action] != "headhunter_home"%>

  <div class="navbar-wagon">
    <!-- Logo -->
    <%= link_to root_path, class: "navbar-talentist-logo" do %>
      <%= image_tag "Logo The talentist-02.png", height: 50 %>
    <% end %>

    <!-- Right Navigation -->
    <div class="navbar-talentist-right hidden-xs hidden-sm">

      <% if headhunter_signed_in? && current_headhunter %>

        <!-- Links when logged in -->
        <% if current_headhunter.validated %>
          <%= link_to repertoire_path, class: "navbar-wagon-item navbar-wagon-link" do %>
            <i class="fas fa-user-friends"></i> RÉPERTOIRE
          <% end %>
        <% end %>
        <% if current_headhunter.mailbox.conversations.count > 0 %>
          <div class="lien-messagerie">
            <%= link_to conversation_path(current_headhunter.mailbox.conversations.last), class: "navbar-wagon-item navbar-wagon-link" do %>
              <i class="fas fa-envelope"></i> MESSAGERIE
              <% if current_user.count_unread_message > 0 %>
                <%= render "shared/notif_message" %>
              <% end %>
            <% end %>
          </div>
        <% end %>

        <% if current_headhunter.validated %>
          <div class="lien-messagerie">
            <%= link_to headhunter_path(current_headhunter), class:"navbar-wagon-item navbar-wagon-link" do %>
              <i class="fa fa-user"></i> PROFIL
              <% if current_headhunter.completed_totaly < 95 %>
                <%= render "shared/notif_point" %>
              <% end %>
            <% end %>
          </div>
        <% end %>

        <!-- Avatar with dropdown menu -->
        <div class="navbar-wagon-item">
          <div class="dropdown">
            <% if current_headhunter.photo.present? %>
              <%= cl_image_tag current_headhunter.photo, class: "avatar dropdown-toggle", id: "navbar-wagon-menu", "data-toggle" => "dropdown" %>
            <% else %>
              <div class="photo-profil avatar dropdown-toggle" id="navbar-wagon-menu" data-toggle="dropdown">
                <div class="initiale">
                  <p style="font-size: 14px; font-weight: 400; margin: 0; color: white;"><%= current_headhunter.firstname.split(//).first.upcase %><%= current_headhunter.name.split(//).first.upcase %></p>
                </div>
              </div>
            <% end %>
            <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
              <li>
                <%= link_to headhunter_path(current_headhunter) do %>
                  <i class="fas fa-sliders-h"></i> <%= t(".profil", default: "Mon compte") %>
                <% end %>
              </li>
              <li>
                <%= link_to edit_headhunter_registration_path do %>
                  <i class="fas fa-cogs"></i></i> <%= t(".profil", default: "Configuration") %>
                <% end %>
              </li>
              <li>
                <%= link_to destroy_headhunter_session_path, method: :delete do %>
                  <i class="fa fa-sign-out"></i>  <%= t(".sign_out", default: "Log out") %>
                <% end %>
              </li>
            </ul>
          </div>
        </div>
      <% elsif talent_signed_in? && current_talent.validated %>
        <%= link_to repertoire_startup_path, class: "navbar-wagon-item navbar-wagon-link" do %>
          <i class="fas fa-rocket"></i> MES START-UPS
        <% end %>

        <% if current_talent.mailbox.conversations.count > 0 %>
          <div class="lien-messagerie">
            <%= link_to conversation_path(current_talent.mailbox.conversations.last), class: "navbar-wagon-item navbar-wagon-link" do %>
              <i class="fas fa-envelope"></i> MESSAGERIE
              <%= render "shared/notif_message" %>
            <% end %>
          </div>
        <% end %>
        <% if current_talent.validated %>
          <%= link_to talent_path(current_talent), class:"navbar-wagon-item navbar-wagon-link" do %>
            <i class="fa fa-user"></i> PROFIL
          <% end %>
        <% end %>
        <!-- Avatar with dropdown menu -->
        <div class="navbar-wagon-item">
          <div class="dropdown">
            <% if current_talent.photo.present? %>
              <%= cl_image_tag current_talent.photo, class: "avatar dropdown-toggle", id: "navbar-wagon-menu", "data-toggle" => "dropdown" %>
            <% else %>
              <div class="photo-profil avatar dropdown-toggle" id="navbar-wagon-menu" data-toggle="dropdown">
                <div class="initiale">
                  <p style="font-size: 14px; font-weight: 400; margin: 0; color: white;"><%= current_talent.firstname.split(//).first.upcase %><%= current_talent.name.split(//).first.upcase %></p>
                </div>
              </div>
            <% end %>
            <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
              <li>
                <%= link_to edit_talent_path(current_talent) do %>
                  <i class="fa fa-user"></i> <%= t(".profil", default: "Profil") %>
                <% end %>
              </li>
              <li>
                <%= link_to edit_talent_registration_path do %>
                  <i class="fas fa-cogs"></i></i> <%= t(".profil", default: "Configuration") %>
                <% end %>
              </li>
              <li>
                <%= link_to destroy_talent_session_path, method: :delete do %>
                  <i class="fa fa-sign-out"></i>  <%= t(".sign_out", default: "Log out") %>
                <% end %>
              </li>
            </ul>
          </div>
        </div>
      <% elsif talentist_signed_in? && current_talentist %>
        <% if current_talentist.admin %>
          <%= link_to "ADMIN", admin_root_path, class: "navbar-wagon-item navbar-wagon-link" %>
        <% end %>

        <%= link_to "START-UP", headhunters_path, class: "navbar-wagon-item navbar-wagon-link" %>

        <%= link_to "TALENTS", talents_path, class: "navbar-wagon-item navbar-wagon-link" %>

        <div class="lien-messagerie">
          <% if current_talentist.messages.count > 0  %>
            <%= link_to "MESSAGERIE", conversation_path(current_talentist.mailbox.conversations.last), class: "navbar-wagon-item navbar-wagon-link" %>
          <% end %>
          <%= render "shared/notif_message" %>
        </div>

        <!-- Avatar with dropdown menu -->
        <div class="navbar-wagon-item">
          <div class="dropdown">
            <% if current_talentist.photo.present? %>
              <%= cl_image_tag current_talentist.photo, class: "avatar dropdown-toggle", id: "navbar-wagon-menu", "data-toggle" => "dropdown" %>
            <% else %>
              <div class="photo-profil avatar dropdown-toggle" id="navbar-wagon-menu" data-toggle="dropdown">
                <div class="initiale">
                  <p style="font-size: 14px; font-weight: 400; margin: 0; color: white;">DM</p>
                </div>
              </div>
            <% end %>
            <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
              <li>
                <%= link_to edit_talentist_registration_path do %>
                  <i class="fas fa-cogs"></i></i> <%= t(".profil", default: "Configuration") %>
                <% end %>
              </li>
              <li>
                <%= link_to destroy_talentist_session_path, method: :delete do %>
                  <i class="fa fa-sign-out"></i>  <%= t(".sign_out", default: "Log out") %>
                <% end %>
              </li>
            </ul>
          </div>
        </div>
      <% else %>
        <%= link_to "REVENIR  À LA PAGE DE CONNEXION", root_path, class: "navbar-wagon-item navbar-wagon-link" %>
      <% end %>
    </div>

    <!-- Dropdown list appearing on mobile only -->
      <!-- <% if current_user && !current_talentist %>
        <% if current_user.validated  %>
          <% if headhunter_signed_in? %>
            <div class="navbar-wagon-item hidden-450px hidden-md hidden-lg">
              <div class="dropdown">
                <i class="fa fa-bars dropdown-toggle" data-toggle="dropdown"></i>
                <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
                  <li><%= link_to headhunter_path(current_headhunter) do %>
                    <i class="fa fa-user"></i> <%= t(".profil", default: "Profil") %>
                  <% end %></li>
                  <li><%= link_to "RÉPERTOIRE", repertoire_path, class: "navbar-wagon-item navbar-wagon-link" %></li>
                  <li>
                    <% if current_headhunter.mailbox.conversations.count > 0 %>
                      <%= link_to "MESSAGERIE", conversation_path(current_headhunter.mailbox.conversations.last), class: "navbar-wagon-item navbar-wagon-link" %>
                      <%= render "shared/notif_message" %>
                    <% else %>
                      <p>Aucun message</p>
                    <% end %>
                  </li>
                  <li><%= link_to destroy_headhunter_session_path, method: :delete do %>
                    <i class="fa fa-sign-out"></i>  <%= t(".sign_out", default: "Log out") %>
                  <% end %></li>
                </ul>
              </div>
            </div>
          <% elsif talent_signed_in?%>
            <div class="navbar-wagon-item hidden-450px hidden-md hidden-lg">
              <div class="dropdown">
                <i class="fa fa-bars dropdown-toggle" data-toggle="dropdown"></i>
                <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
                  <li><%= link_to talent_path(current_talent) do %>
                    <i class="fa fa-user"></i> <%= t(".profil", default: "Profil") %>
                  <% end %></li>
                  <li><%= link_to "RÉPERTOIRE", repertoire_startup_path, class: "navbar-wagon-item navbar-wagon-link" %></li>
                  <li>
                    <% if current_talent.mailbox.conversations.count > 0 %>
                      <%= link_to "MESSAGERIE", conversation_path(current_talent.mailbox.conversations.last), class: "navbar-wagon-item navbar-wagon-link" %>
                      <%= render "shared/notif_message" %>
                    <% else %>
                      <p>Aucun message</p>
                    <% end %>
                  </li>
                  <li><%= link_to destroy_talent_session_path, method: :delete do %>
                    <i class="fa fa-sign-out"></i>  <%= t(".sign_out", default: "Log out") %>
                  <% end %></li>
                </ul>
              </div>
            </div>
          <% end %>
        <% end %>
      <% end %> -->












    <!-- Menu appearing at page bottom -->
    <% if current_user && !current_talentist %>
      <% if current_user.validated  %>


        <div class="bottom-menu">
          <% if headhunter_signed_in? %>

            <!-- Links when logged in -->
            <div class="bottom-menu-item">
              <%= link_to "RÉPERTOIRE", repertoire_path, class: "navbar-wagon-item navbar-wagon-link" %>
            </div>
            <% if current_headhunter.mailbox.conversations.count > 0 %>
              <div class="bottom-menu-item lien-messagerie">
                <%= link_to "MESSAGERIE", conversation_path(current_headhunter.mailbox.conversations.last), class: "navbar-wagon-item navbar-wagon-link lien-messagerie" %>
                <% if current_user.count_unread_message > 0 %>
                  <%= render "shared/notif_message" %>
                <% end %>
              </div>
            <% else %>
              <div class="bottom-menu-item">
                <p>Aucun message</p>
              </div>
            <% end %>
            <div class="bottom-menu-item">
              <div class="navbar-wagon-item">
                <div class="dropup">
                  <i class="fa fa-bars dropdown-toggle" id="navbar-wagon-menu" data-toggle="dropdown"></i>
                  <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
                    <li class="lien-messagerie">
                      <%= link_to headhunter_path(current_headhunter) do %>
                        <i class="fa fa-user"></i> <%= t(".profil", default: "Profil") %>
                        <% if current_headhunter.completed_totaly < 95 %>
                          <%= render "shared/notif_point" %>
                        <% end %>
                      <% end %>
                    </li>
                    <li>
                      <%= link_to destroy_headhunter_session_path, method: :delete do %>
                        <i class="fa fa-sign-out"></i>  <%= t(".sign_out", default: "Log out") %>
                      <% end %>
                    </li>
                  </ul>
                </div>
              </div>
            </div>

          <% elsif talent_signed_in?%>

            <div class="bottom-menu-item">
              <%= link_to "START-UP", repertoire_startup_path, class: "navbar-wagon-item navbar-wagon-link" %>
            </div>
            <% if current_talent.mailbox.conversations.count > 0 %>
              <div class="bottom-menu-item lien-messagerie">
                <%= link_to conversation_path(current_talent.mailbox.conversations.last), class: "navbar-wagon-item navbar-wagon-link" do %>
                  MESSAGERIE
                  <%= render "shared/notif_message" %>
                <% end %>
              </div>
            <% else %>
              <div class="bottom-menu-item">
                <p class="navbar-wagon-item navbar-wagon-link gray no-margin">MESSAGERIE</p>
              </div>
            <% end %>
            <div class="bottom-menu-item">
              <div class="navbar-wagon-item">
                <div class="dropup">
                  <i class="fa fa-bars dropdown-toggle" id="navbar-wagon-menu" data-toggle="dropdown"></i>
                  <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
                    <li>
                      <%= link_to talent_path(current_talent) do %>
                        <i class="fa fa-user"></i> <%= t(".profil", default: "Profil") %>
                      <% end %>
                    </li>
                    <li>
                      <%= link_to destroy_talent_session_path, method: :delete do %>
                        <i class="fa fa-sign-out"></i>  <%= t(".sign_out", default: "Log out") %>
                      <% end %>
                    </li>
                  </ul>
                </div>
              </div>
            </div>

          <% end %>
        </div>
      <% end %>
    <% end %>
  </div>

  <div class="navbar-wagon-margin" style="margin-top: 70px"></div>


<% end %>





